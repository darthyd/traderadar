{
  "name": "trader-radar",
  "version": "1.0.1",
  "description": "A simple, easy to use, and fast radar for traders.",
  "main": "src/main.js",
  "scripts": {
    "start": "electron .",
    "build:linux": "electron-packager ./ trader-radar --platform linux --arch x64 --out build/linux/trader-radar/1.0.0 --app-version 1.0.0 --overwrite",
    "build:linux32": "electron-packager ./ trader-radar --platform linux --arch ia32 --out build/linux32/trader-radar/1.0.0 --app-version 1.0.0 --overwrite",
    "build:mac": "electron-packager ./ trader-radar --platform darwin --arch x64 --icon ./assets/icon.icns --out build/mac/trader-radar/1.0.0 --app-version 1.0.0 --overwrite",
    "build:win": "electron-packager ./ trader-radar --platform win32 --arch x64 --out build/windows/trader-radar/1.0.0 --app-version 1.0.0 --overwrite",
    "build:win32": "electron-packager ./ trader-radar --platform win32 --arch ia32 --out build/windows32/trader-radar/1.0.0 --app-version 1.0.0 --overwrite",
    "build:all": "npm run build:linux && npm run build:linux32 && npm run build:mac && npm run build:win && npm run build:win32"
  },
  "keywords": [
    "Electron",
    "Webview",
    "Chromium",
    "Linux",
    "Initial",
    "Alpha",
    "Trading"
  ],
  "author": "Lucas Oliveira",
  "license": "CC0-1.0",
  "devDependencies": {
    "electron": "^13.1.0",
    "electron-packager": "^15.2.0"
  }
}
